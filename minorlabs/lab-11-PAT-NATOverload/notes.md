Nat inside means that ip address from the inside get converted into NAT, or in this case PAT since it is more common then NAT
PAT is simply NAT overload, and it is many private addresses translated into one, with the unique ID being the port number.
First Server Config yay
